# ============================================================
# Data Architecture — Dépendances Python
# ============================================================

# --- Traitement données ---
pandas>=2.0
pyarrow>=14.0

# --- Géospatial (Silver : jointures spatiales, Gold : géométries) ---
geopandas>=0.14
shapely>=2.0

# --- HTTP & APIs ---
requests>=2.31
urllib3>=2.0

# --- API REST (exposition Gold) ---
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0

# --- Variables d'environnement ---
python-dotenv>=1.0

# --- Micro-batch mobilité (Vélib' + PRIM IDFM) ---
schedule>=1.2

# --- PostgreSQL (export Gold — critique pour évaluation) ---
sqlalchemy>=2.0
psycopg2-binary>=2.9        # driver PostgreSQL (binaire, sans compilation)
# geoalchemy2>=0.14          # décommenter si PostGIS est installé (géométries natives)

# --- Sécurité API : quotas / rate limiting (C2.1) ---
slowapi>=0.1.9

# --- NoSQL document store (C1.2) — MongoDB ---
pymongo>=4.6

# --- Tests de charge (Locust) ---
locust>=2.20
